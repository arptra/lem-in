1. Сделать обработку ошибоку.
    в файле input.c функция:
        static void	parse_line(char *line, t_room *input, t_graph *graph):
    Внутри нее помечено коментариями где можно вставить обработку ошибок.
    Если необхоимо можно переписать весь парсер заново.
    Ошибки которые нужно обрабатывать.
        1) Количество муравьев не отрицательное.
        2) Ровно по одной вершине обознаены как старт и как финиш.
        3) Есть хотя бы один путь соединяющий старт и финиш, т.е. граф является связным.
            это может сделать функция ssp_finder(graph->start);
            если после ее выполнения поле graph->end->parent != NULL, то хотя бы один путь найден.
            если graph->end->parent == NULL, то от вершины START до END добраться нельзя.
        4) нет вершин с одинаковыми названиями.
        5) Нет вершин с одинаковыми координатами (одинаковые и х и у).
        6) Ребра соединяют только существующие вершины.
        7) Нет вершин, начинающихся с 'L' и содержащих дефис.
